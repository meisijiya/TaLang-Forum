[pytest]
testpaths = testcase
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --strict-markers --color=yes
markers =
    captcha_blocked: tests blocked by captcha verification
    slow: slow tests (>30s)
filterwarnings =
    ignore::DeprecationWarning
